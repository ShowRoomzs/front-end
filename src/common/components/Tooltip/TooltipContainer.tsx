import { View } from "react-native";

import { TooltipInstance } from "@/common/providers/TooltipProvider";

export default function TooltipContainer(props: TooltipInstance) {
  const { config, isOpen, tooltipRef } = props;

  return <View></View>;
}
